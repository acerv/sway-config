install_data([
    'background.jpg',
    'config',
  ],
  install_dir: join_paths(home_dir, 'sway'),
)
install_data('env', install_dir: '/etc/sway')
install_data('sway.desktop', install_dir: '/usr/local/share/wayland-sessions/')
